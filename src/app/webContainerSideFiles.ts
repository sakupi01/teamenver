import { FileSystemTree } from '@webcontainer/api'

export const files: FileSystemTree = {
  'index.js': {
    file: {
      contents: `
      // 🌸 Here's the full code which is going to be executed in the terminal.
      `,
    },
  },
}
